/**
 * Config Package
 *
 * Application configuration described with `effect`'s `Config` module.
 * Values are read from the environment with sensible defaults, so services
 * never hardcode credentials, URLs, or tuning knobs.
 */

export * from "./http";
export * from "./tmdb";
